google-adk[a2a]>=1.31.1,<3.0.0
mcp>=1.24.0,<2.0.0
google-genai>=1.27.0,<3.0.0
python-dotenv>=1.0.0,<2.0.0
google-cloud-aiplatform[agent_engines]>=1.112.0,<2.0.0
db-dtypes>=1.0.0,<2.0.0
google-cloud-storage>=2.14.0,<4.0.0
a2ui-agent-sdk @ git+https://github.com/google/A2UI.git@ade478faf8dcad611b5efb6b864dcbfbc4a51f68#subdirectory=agent_sdks/python
a2a-sdk>=0.3.4,<0.4.0
google-cloud-scheduler>=2.0.0,<3.0.0
google-cloud-tasks>=2.13.0,<3.0.0
google-cloud-pubsub>=2.0.0,<3.0.0
google-cloud-firestore>=2.16.0,<3.0.0
google-cloud-logging>=3.0.0,<4.0.0
opentelemetry-api>=1.20.0,<2.0.0

# --- Computer Use / browser automation only (enableComputerUse) ---
# Uncomment all four together when the demo enables Computer Use, and uncomment the
# matching Playwright layer in the Dockerfile. Leave them commented otherwise: they
# pull a much heavier resolution than the base set needs.
# google-genai>=2.7.0 narrows the base google-genai line above rather than replacing
# it. It is only satisfiable with recent google-cloud-aiplatform releases, and those
# depend on Google Cloud OTel packages whose satisfying versions are pre-releases (1.12.0a0).
# uv ignores pre-release markers on transitive deps, so the two otel lines must be
# declared directly or the resolver rejects every aiplatform that allows genai 2.x.
# playwright==1.55.0
# google-genai>=2.7.0,<3.0.0
# opentelemetry-exporter-gcp-logging>=1.12.0a0,<2.0.0
# opentelemetry-resourcedetector-gcp>=1.12.0a0,<2.0.0
