# Local configuration — never commit real keys or project ids
.env
.env.*
!.env.example

# Python
__pycache__/
*.py[cod]
.venv/
venv/
*.egg-info/

# Tooling caches
.pytest_cache/
.ruff_cache/
.mypy_cache/

# ADK local session state
.adk/
*.db
*.sqlite

# Local runtime state
*.log
.prism-port

# Credentials (belt and braces)
*.pem
*.key
*-credentials.json
service-account*.json

# OS
.DS_Store
