import { FormatAgentNamePipe } from './format-agent-name.pipe';

describe('FormatAgentNamePipe', () => {
  it('create an instance', () => {
    const pipe = new FormatAgentNamePipe();
    expect(pipe).toBeTruthy();
  });
});
